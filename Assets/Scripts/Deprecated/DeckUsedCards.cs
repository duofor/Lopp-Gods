using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DeckUsedCards : MonoBehaviour {

    [SerializeField] private List<Card> deckUsedCards;

    void Start() {
        // deckUsedCards = GameController.instance.deck.getUsedCards();
    }

    void Update() {
        // deckUsedCards = GameController.instance.deck.getUsedCards();
    }

    

}
